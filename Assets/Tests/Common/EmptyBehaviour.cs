namespace Mirage.Tests.Runtime
{
    /// <summary>
    /// Use this when tests needs a NetworkBehaviour but doesn't need to test it
    /// </summary>
    public class EmptyBehaviour : NetworkBehaviour { }
}
