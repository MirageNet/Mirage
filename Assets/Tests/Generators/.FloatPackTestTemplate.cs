// DO NOT EDIT: GENERATED BY FloatPackTestGenerator.cs

using System;
using System.Collections;
using Mirage.Serialization;
using Mirage.Tests.Runtime.ClientServer;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace Mirage.Tests.Runtime.Generated.FloatPackAttributeTests
{
    public class FloatPackBehaviour_%%NAME%% : NetworkBehaviour
    {
        [FloatPack(%%PACKER_ATTRIBUTE%%)]
        [SyncVar] public float myValue;
    }
    public class FloatPackTest_%%NAME%% : ClientServerSetup<FloatPackBehaviour_%%NAME%%>
    {
        const float value = %%VALUE%%;
        const float within = %%WITHIN%%;

        [Test]
        public void SyncVarIsBitPacked()
        {
            serverComponent.myValue = value;

            using (PooledNetworkWriter writer = NetworkWriterPool.GetWriter())
            {
                serverComponent.SerializeSyncVars(writer, true);

                Assert.That(writer.BitPosition, Is.EqualTo(%%BIT_COUNT%%));

                using (PooledNetworkReader reader = NetworkReaderPool.GetReader(writer.ToArraySegment()))
                {
                    clientComponent.DeserializeSyncVars(reader, true);
                    Assert.That(reader.BitPosition, Is.EqualTo(%%BIT_COUNT%%));

                    Assert.That(clientComponent.myValue, Is.EqualTo(value).Within(within));
                }
            }
        }
    }
}
