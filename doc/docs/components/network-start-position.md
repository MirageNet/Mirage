# Network Start Position

This component has been removed from Mirage. Its functionality for defining player spawn positions is now handled directly by the `NetworkManager` or through custom spawn handlers.